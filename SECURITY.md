# Security Policy

## 支持范围

项目尚处于 `0.x` 本地开发阶段。安全修复优先应用到最新主分支；早期预览版本不承诺长期安全更新。

## 私下报告漏洞

请不要在公开 Issue、Discussion、截图或日志中披露可利用漏洞、API Key、个人数据或访问令牌。

仓库公开后，请优先使用 GitHub Private Vulnerability Reporting。若该功能尚未启用，请通过仓库所有者 GitHub 资料中列出的私密联系方式联系维护者，并只发送最小必要信息。

报告建议包括：

- 受影响版本或提交；
- 可重复的最小步骤；
- 实际与潜在影响；
- 已知缓解方法；
- 是否涉及未成年人数据、人物素材或第三方服务凭据。

维护者会确认收到报告、评估影响并协调修复与披露时间。请在修复发布前避免公开细节。

## 重点风险

本项目特别关注文件上传、文档提示词注入、SSRF、人物包 ZIP、对象存储访问、API Key 泄漏、越权读取人物私聊、未成年人隐私和媒体授权绕过。

## 已知依赖审计项

截至2026-07-16，`npm audit` 会通过 Next.js 15.5.20 的固定依赖报告 PostCSS `GHSA-qx2v-qp2m-jg93`（中等级）。当前应用不接收或运行用户提供的 CSS，但项目仍会跟踪上游修复。`npm audit fix --force` 建议降级到过时的 Next.js 9.3.3，因此不得使用该强制修复；CI 对高危与严重问题保持阻断，Dependabot 负责跟踪可用升级。
