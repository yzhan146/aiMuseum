import { sendThreadMessage } from "@/lib/chat-service";
import { resolveIdentity, withIdentity } from "@/lib/identity";
import { getThreadForUser, listMessages } from "@/lib/platform-store";
export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { const identity = resolveIdentity(request); const { id } = await params; const url = new URL(request.url); const result = listMessages(id, identity.userId, url.searchParams.get("cursor") ?? undefined, Number(url.searchParams.get("limit") ?? 50)); return withIdentity(result ? Response.json(result) : Response.json({ error: "线程不存在" }, { status: 404 }), identity); }
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const identity = resolveIdentity(request); try { const { id } = await params; const thread = getThreadForUser(id, identity.userId); if (!thread) return withIdentity(Response.json({ error: "线程不存在" }, { status: 404 }), identity); const body = await request.json(); if (!body.message?.trim() || body.message.length > 2000) return withIdentity(Response.json({ error: "问题无效" }, { status: 400 }), identity); const result = await sendThreadMessage(identity.userId, thread, body.message, body.ageBand, body.locale); if (request.headers.get("accept")?.includes("text/event-stream")) { const stream = new ReadableStream({ start(controller) { const encoder = new TextEncoder(); for (const chunk of result.answer.match(/.{1,12}/gu) ?? []) controller.enqueue(encoder.encode(`event: token\ndata: ${JSON.stringify(chunk)}\n\n`)); controller.enqueue(encoder.encode(`event: result\ndata: ${JSON.stringify(result)}\n\n`)); controller.close(); } }); return withIdentity(new Response(stream, { headers: { "content-type": "text/event-stream; charset=utf-8", "cache-control": "no-cache" } }), identity); } return withIdentity(Response.json(result), identity); } catch (error) { return withIdentity(Response.json({ error: error instanceof Error ? error.message : "对话失败" }, { status: 409 }), identity); } }
