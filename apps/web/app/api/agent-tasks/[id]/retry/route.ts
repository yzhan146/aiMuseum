import { retryTask } from "@/lib/agents";import { resolveIdentity,withIdentity } from "@/lib/identity";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const identity=resolveIdentity(request);try{const{id}=await params;const task=retryTask(identity.userId,id);return withIdentity(task?Response.json({task}):Response.json({error:"任务不存在"},{status:404}),identity)}catch(error){return withIdentity(Response.json({error:error instanceof Error?error.message:"重试失败"},{status:409}),identity)}}
